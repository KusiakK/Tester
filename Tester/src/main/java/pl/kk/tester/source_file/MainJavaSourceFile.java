package pl.kk.tester.source_file;

import org.apache.velocity.Template;
import org.apache.velocity.VelocityContext;

class MainJavaSourceFile extends AbstractJavaSourceFile {
    MainJavaSourceFile(VelocityContext velocityContext, Template template) {
        super(velocityContext, template);
    }
}
